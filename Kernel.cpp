
#include <string> 
#include <iostream> 
using namespace std;

int main() {
    // while loop taking commands 
    string command; 
    
    while(true) {
        cin >> command;
        if (command == "FM") {

        } else if (command == "NF") {

        } else if (command == "MF") {

        } else if (command == "DF") {

        } else if (command == "DB") {
            
        }
    }

    return 0; 
}